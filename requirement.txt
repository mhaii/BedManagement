Django >= 1.9.1
PyYAML >= 3.11
# django-angular 0.7.15 doesn't support Django 1.9, so we pulled it from github instead
# django-angular >= 0.7.16
git+https://github.com/jrief/django-angular.git#egg=django_angular-master
django-bower >= 5.0.4
django_compressor >= 1.6
django-libsass >= 0.6
setuptools >= 19.2
django-appconf >= 1.0.1
# because MySQL-python is lying as fuck
mysqlclient >= 1.3.7
